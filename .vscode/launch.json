{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug Ticker Service",
        "type": "python",
        "request": "launch",
        "module": "uvicorn",
        "args": [
          "app.main:app",
          "--reload",
          "--port",
          "8000"
        ],
        "envFile": "${workspaceFolder}/.env",
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  